# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
ENV/

# Local env files
.env
.env.*
!.env.example
env.worker.local

# TLS material generated on the VM by setup.sh (do not commit real keys)
deploy/ssl/*.pem
deploy/.htpasswd

# Scrape job snapshots (./data/scrape_jobs on host when using docker-compose volume)
data/scrape_jobs/*.json

# OS / editor
.DS_Store
Thumbs.db
